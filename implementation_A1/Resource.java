/**
 * Resources enumeration, better implementation than Switch cases.
 * It will be used later on by the other Enum of Terrain to map
 * each terrain with the resource it produces.
 */

public enum Resource {
    WOOL,
    BRICK,
    ORE,
    LUMBER,
    GRAIN
}
